#include "header.h"
/*
abr* creationabr(char* l)
{
	verifpchar(l);

	abr* nv = malloc(sizeof(abr));
	nv->fg = NULL;
	nv->fd = NULL;
	nv->taille_ligne = 0;
	nv->nbr_moyenne[0] = 1;
	nv->nbr_moyenne[1] = 1;

	return nv;
}



abr* insertabr(abr* a, abr* nv)
{
	verifpabr(nv);

	if(a == NULL)
	{
		a = nv;
		return a;
	}
	/*else if(infstrictabr(nv,a))
	{
		if(a->fg)
			return insertabr(a,nv);
		else
			a->fg = nv;
	}
	else if(infstrictabr(a,nv))
	{
		if(a->fd)
			return insertabr(a,nv);
	}//////////////////////////////////////  FAUT FAIRE LE CAS OU C EGAL ??????????????????
	return a;	
}
*/
